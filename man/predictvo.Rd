% Generated by roxygen2 (4.1.1): do not edit by hand
% Please edit documentation in R/predictvo.R
\name{predictvo}
\alias{predictvo}
\title{Predict the relation of objects to a list of variables of interest}
\usage{
predictvo(matrix, reltable, mvars)
}
\arguments{
\item{matrix}{data.frame with numeric values}

\item{reltable}{table of correlation values (e.g. produced by \code{reltable()})}

\item{mvars}{vector of variables of interest (full name)}
}
\value{
table with predicted, normalized relation values of every object and given
variables of interest
}
\description{
By comparison of significant correlations within variables and the variables the objects
incorporate \code{predictvo()} makes an prediction about the relation to given variables
for every object.
}
\examples{
testmatrixrand <- data.frame(
   matrix(base::sample(0:1,400,replace=T), nrow=20, ncol=20)
)

testcorr <- corrmat(testmatrixrand, "lambda", chi2limit = 0.1, dim = 1)

rel <- reltable(testcorr)

predictvo(testmatrixrand, rel, c("X1", "X2", "X3"))
}

